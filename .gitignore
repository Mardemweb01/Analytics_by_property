__pycache__/
*.pyc
venv/
.venv/
*.xlsx

# Datos reales sin cifrar y la clave de cifrado -- nunca van al repo publico.
# Solo se commitean los .json.enc (ver cifrar.py / descifrar.py).
data/*.json
data/*/
clave.key

# Parquet intermedios que genera cargar_a_fabric.py antes de subir a OneLake.
# Son datos financieros reales en claro.
.carga/
